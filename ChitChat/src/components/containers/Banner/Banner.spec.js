import React from "react";
import { shallow } from "enzyme";
import Banner from "./Banner";

jest.useFakeTimers();

describe("List Item tests", () => {

  it("should be 12", () => {
    expect(12).toBe(12);
  });

});
